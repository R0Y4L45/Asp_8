@model IQueryable<BookViewModel>

<div class="container">
    <div class="text-center">
        <h1 class="bg-warning">Admin page</h1>
    </div>


    @if (TempData["N"] != null && TempData["N"]?.ToString() != string.Empty)
    {
        if (TempData["N"]?.ToString()?.Last() == ')')
        {
            <div class="alert alert-success">
                <strong>Success!</strong> @TempData["N"]?.ToString()
            </div>
        }
        else if (TempData["N"]?.ToString()?.Last() == '(')
        {
            <div class="alert alert-danger">
                <strong>Success!</strong> @TempData["N"]?.ToString()
            </div>
        }

        TempData["N"] = string.Empty;
    }

    <div class="row d-flex align-content-around">
        <div class="col-md-6 my-lg-5">
            <a class="btn btn-primary btn-lg btn-block" asp-area="Admin" asp-controller="AdminBookStore" asp-action="Add">Add New</a>
        </div>

        <div class="col-md-6 my-lg-5">
            <a class="btn btn-primary btn-lg btn-block" asp-area="User" asp-controller="BookStore" asp-action="Main">Go to User</a>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-12">
            <div class="table-responsive">
                <table style="min-height: 150px;" class="table table-striped table-dark table-hover">
                    <thead class="thead-dark">
                        <tr>
                            <th class="text-center" scope="col">Id</th>
                            <th class="text-center" scope="col">Name</th>
                            <th class="text-center" scope="col">Category</th>
                            <th class="text-center" scope="col">Theme</th>
                            <th class="text-center" scope="col">Author</th>
                            <th class="text-center" scope="col">Press</th>
                            <th class="text-center" scope="col">Price</th>
                            <th class="text-center" scope="col">Count</th>
                            <th class="text-center" scope="col">Description</th>
                            <th class="text-center" scope="col" style="width: 200px;">Action</th>
                        </tr>
                    </thead>
                    <books Area=true Items="@Model"></books>
                </table>
            </div>
        </div>
    </div>
</div>